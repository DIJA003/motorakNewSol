@using Motorak.BLL.ModelVM.Customer;
@model List<CustomerListModel>
@{
    ViewData["Title"] = "Customer Management";
    Layout = "_MotorakIndex";
}

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/3.4.0/remixicon.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Manage.css">
</head>
<body>
    <!-- Display Success/Error Messages -->
    @if (TempData["Success"] != null)
    {
        <div class="notification show" style="background: var(--success-color);">
            <div class="notification-content">
                <i class="ri-check-line"></i>
                <span>@TempData["Success"]</span>
            </div>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="notification show" style="background: var(--error-color);">
            <div class="notification-content">
                <i class="ri-close-circle-line"></i>
                <span>@TempData["Error"]</span>
            </div>
        </div>
    }
    @if (TempData["Info"] != null)
    {
        <div class="notification show" style="background: var(--info-color);">
            <div class="notification-content">
                <i class="ri-information-line"></i>
                <span>@TempData["Info"]</span>
            </div>
        </div>
    }

    <!-- Customer Index View -->
    <section class="customers-section">
        <div class="container">
            <!-- Header -->
            <div class="section-header">
                <div class="header-content">
                    <h1 class="section-title">
                        <i class="ri-team-line"></i>
                        Customer Management
                    </h1>
                    <p class="section-subtitle">Manage your customers efficiently</p>
                </div>
                <div class="header-actions">
                    <div class="search-container">
                        <input type="text" id="searchInput" placeholder="Search customers..." class="search-input">
                        <i class="ri-search-line search-icon"></i>
                    </div>
                    <a href="/Customer/Create" class="btn-primary">
                        <i class="ri-add-line"></i>
                        Add New Customer
                    </a>
                </div>
            </div>

            <!-- Stats Cards -->
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon total">
                        <i class="ri-team-line"></i>
                    </div>
                    <div class="stat-content">
                        <h3 class="stat-number">@Model.Count</h3>
                        <p class="stat-label">Total Customers</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon active">
                        <i class="ri-user-check-line"></i>
                    </div>
                    <div class="stat-content">
                        <h3 class="stat-number">@Model.Count(c => !c.IsDeleted)</h3>
                        <p class="stat-label">Active Customers</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon new">
                        <i class="ri-user-add-line"></i>
                    </div>
                    <div class="stat-content">
                        <h3 class="stat-number">@Model.Count(c => c.CreatedAt.Date == DateTime.Today)</h3>
                        <p class="stat-label">New Today</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon purchases">
                        <i class="ri-shopping-bag-line"></i>
                    </div>
                    <div class="stat-content">
                        <h3 class="stat-number">@Model.Sum(c => c.PurchasedCarsCount)</h3>
                        <p class="stat-label">Total Purchases</p>
                    </div>
                </div>
            </div>

            @if (Model.Any())
            {
                <div class="customers-grid" id="customersGrid">
                    @foreach (var customer in Model)
                    {
                        <div class="customer-card" data-customer-id="@customer.Id">
                            <div class="card-header">
                                <div class="customer-avatar">
                                    @customer.Name.First().ToString().ToUpper()
                                </div>
                                <div class="customer-info">
                                    <h3 class="customer-name">@customer.Name</h3>
                                    <p class="customer-email">@customer.Email</p>
                                </div>
                                <div class="customer-status @(customer.IsDeleted ? "deleted" : "active")">
                                    <i class="@(customer.IsDeleted ? "ri-close-circle-line" : "ri-check-circle-line")"></i>
                                </div>
                            </div>
                            <div class="card-content">
                                <div class="info-grid">
                                    <div class="info-item">
                                        <i class="ri-phone-line"></i>
                                        <span>@(customer.Phone ?? "No phone")</span>
                                    </div>
                                    <div class="info-item">
                                        <i class="ri-shopping-bag-line"></i>
                                        <span>@customer.PurchasedCarsCount Cars</span>
                                    </div>
                                    <div class="info-item">
                                        <i class="ri-calendar-line"></i>
                                        <span>@customer.CreatedAt.ToString("MMM dd, yyyy")</span>
                                    </div>
                                    @if (customer.IsUpdated)
                                    {
                                        <div class="info-item">
                                            <i class="ri-edit-line"></i>
                                            <span>Updated @customer.UpdatedAt.ToString("MMM dd, yyyy")</span>
                                        </div>
                                    }
                                    @if (customer.IsDeleted)
                                    {
                                        <div class="info-item">
                                            <i class="ri-delete-bin-line"></i>
                                            <span>Deleted @customer.DeletedAt?.ToString("MMM dd, yyyy")</span>
                                        </div>
                                    }
                                </div>
                            </div>
                            <div class="card-actions">
                                <button class="action-btn view-btn" onclick="viewCustomer('@customer.Id')">
                                    <i class="ri-eye-line"></i> View
                                </button>
                                @if (!customer.IsDeleted)
                                {
                                    <button class="action-btn edit-btn" onclick="editCustomer('@customer.Id')">
                                        <i class="ri-edit-line"></i> Edit
                                    </button>
                                    <button class="action-btn delete-btn" onclick="deleteCustomer('@customer.Id')">
                                        <i class="ri-delete-bin-line"></i> Delete
                                    </button>
                                }
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="no-customers-message" style="text-align: center; padding: 3rem; background: rgba(255,255,255,0.1); border-radius: 1rem; backdrop-filter: blur(10px);">
                    <i class="ri-user-line" style="font-size: 3rem; color: rgba(255,255,255,0.5); margin-bottom: 1rem;"></i>
                    <h3 style="color: white; margin-bottom: 1rem;">No Customers Found</h3>
                    <p style="color: rgba(255,255,255,0.7); margin-bottom: 2rem;">Start by adding your first customer to the system.</p>
                    <a href="/Customer/Create" class="btn-primary">
                        <i class="ri-add-line"></i>
                        Add First Customer
                    </a>
                </div>
            }
        </div>
    </section>

    <!-- Delete Confirmation Modal -->
    <div id="deleteModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3><i class="ri-error-warning-line"></i> Confirm Deletion</h3>
                <span class="close" onclick="closeDeleteModal()">&times;</span>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this customer? This action will mark the customer as deleted but can be restored later.</p>
                <div class="modal-actions">
                    <button class="btn-secondary" onclick="closeDeleteModal()">Cancel</button>
                    <button class="btn-danger" id="confirmDeleteBtn">Delete Customer</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Notification -->
    <div id="notification" class="notification">
        <div class="notification-content">
            <i class="ri-check-line"></i>
            <span id="notification-message">Success!</span>
        </div>
    </div>

    <!-- Anti-forgery token for AJAX requests -->
    @Html.AntiForgeryToken()

    @section Scripts {
        <script src="~/js/Manage/common.js"></script>
        <script src="~/js/Manage/list.js"></script>
    }

</body>
</html>